package com.progettarsi.vibemusic.ui.theme

import androidx.compose.ui.graphics.Color

// --- PRIMARY (Il tuo Brand) ---
val PurplePrimary = Color(0xFF6C5DD3)      // Il viola principale (bottoni, icone attive)
val PurpleDark = Color(0xFF5145A0)         // Variante più scura per sfumature
val PurpleLight = Color(0xFF9A8DF5)        // Variante pastello per testi colorati su sfondo scuro

// --- SECONDARY (Accenti) ---
val CyanAccent = Color(0xFF00D4FF)         // Un azzurro elettrico per contrastare il viola (opzionale)
val PinkAccent = Color(0xFFFF4081)         // Per i cuori/like attivi

// --- BACKGROUNDS (La parte cruciale per il Dark Mode) ---
val DarkBackground = Color(0xFF121212)
val BlackVoid = Color(0xFF0A0A0A)          // Nero quasi assoluto (per lo sfondo Activity)
val SurfaceDark = Color(0xFF161618)        // Grigio molto scuro (per le card)
val SurfaceHighlight = Color(0xFF252530)   // Grigio con punta di viola (per il Dock e elementi in primo piano)

// --- TEXT ---
val TextWhite = Color(0xFFFFFFFF)          // Titoli
val TextGrey = Color(0xFF8E8E93)           // Sottotitoli/Artisti (Apple style grey)
val TextGhost = Color(0xFFB0B0C0)          // Testi secondari luminosi